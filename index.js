import { ethers } from 'ethers';

const value = "unknown remain reject better oat buyer afford liar claim step cheap cheese";
const phrase = ethers.utils.isValidMnemonic(value);

if(phras){
    console.log("yes");
} else {
    console.log("no");
}